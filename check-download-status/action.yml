name: 'Check Download Status'
description: 'Checks download status of marked downloads'
author: 'd10sfan'
runs:
    using: 'node24'
    main: 'dist/index.js'
outputs:
  matrix:
    description: Matrix
