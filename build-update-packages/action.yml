name: 'Update Packages JSON'
description: 'Update packages.json file'
author: 'd10sfan'
runs:
    using: 'node24'
    main: 'dist/index.js'
inputs:
  matrix:
    description: Generated matrix from earlier that contains file names
    required: true
  version:
    description: Current version
    required: true
  app_ids:
    description: Set of app ids for the script
    required: true
